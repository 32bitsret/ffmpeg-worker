[phases.setup]
# freefont_ttf, liberation_ttf, dejavu_fonts kept as fallbacks — bundled fonts in ./fonts/ take priority
nixPkgs = ["nodejs_20", "ffmpeg", "freefont_ttf", "liberation_ttf", "dejavu_fonts", "fontconfig"]

[phases.install]
cmds = ["npm ci"]

[start]
cmd = "node index.js"
